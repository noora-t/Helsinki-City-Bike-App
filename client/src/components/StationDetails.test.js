// import { render, waitFor } from '@testing-library/react';
import { StationDetails } from './StationDetails';

describe(StationDetails, () => {
  test('renders the station details', async () => {

  });
});