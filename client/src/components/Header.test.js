import { render, screen } from '@testing-library/react';
import { Header } from './Header';

describe(Header, () => {
    it("has a header", () => {
        // render(<Header />);
        // expect(screen.findByText("Helsinki City Bike App")).toBeInTheDocument();
    });
});
